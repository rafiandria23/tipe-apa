{
  "name": "tipe-apa",
  "version": "0.1.0",
  "description": "Simple JavaScript data type checker.",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "dev": "NODE_ENV=development tsc --watch",
    "clean": "rm -rf ./dist",
    "build": "tsc",
    "test": "yarn clean && tsc && jest",
    "publish:patch": "yarn build && npm version patch && git push origin master && npm publish",
    "publish:minor": "yarn build && npm version minor && git push origin master && npm publish",
    "publish:major": "yarn build && npm version major && git push origin master && npm publish"
  },
  "repository": "https://github.com/rafiandria23/tipe-apa.git",
  "author": "Adam Rafiandri",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "jest": "^25.3.0",
    "ts-jest": "^25.3.1",
    "tslib": "^1.11.1",
    "typescript": "^3.8.3"
  }
}
