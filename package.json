{
  "name": "tipe-apa",
  "version": "0.0.1",
  "description": "Simple JavaScript data type checker.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "NODE_ENV=development tsc --watch",
    "clean": "rm -rf ./dist",
    "build": "tsc",
    "test": "yarn clean && tsc && jest",
    "publish:patch": "npm patch && git push origin master && npm publish",
    "publish:minor": "npm minor && git push origin master && npm publish",
    "publish:major": "npm major && git push origin master && npm publish"
  },
  "repository": "https://github.com/rafiandria23/tipe-apa.git",
  "author": "Adam Rafiandri",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "jest": "^25.3.0",
    "ts-jest": "^25.3.1",
    "tslib": "^1.11.1",
    "typescript": "^3.8.3"
  }
}
